---
---
<h2>Наши ключевые преимущества</h2>
<div class="advantage-grid">
    <div class="adv-card">
        <h4>Точность AI-Расчетов</h4>
        <p>Использование алгоритмов машинного обучения позволяет проводить гидравлические и тепловые расчеты с точностью до 99.8%, исключая человеческие ошибки.</p>
    </div>
    <div class="adv-card">
        <h4>Гарантия Согласования</h4>
        <p>Мы берем на себя взаимодействие с РСО. Проекты IT37 всегда соответствуют актуальным требованиям ГОСТ и СП, что гарантирует успешное введение в эксплуатацию.</p>
    </div>
    <div class="adv-card">
        <h4>3D-Визуализация</h4>
        <p>Разрабатываем детальные 3D-модели узлов учета, что упрощает монтажные работы, снижает риск коллизий и сокращает сроки реализации проекта.</p>
    </div>
</div>

<style>
    .advantage-grid {
        display: grid; 
        grid-template-columns: repeat(3, 1fr); 
        gap: 20px; 
        margin-top: 50px;
    }
    .adv-card {
        background: var(--bg-deep); 
        border: 1px solid var(--border); 
        padding: 25px; 
        transition: transform 0.3s, border-color 0.3s;
    }
    .adv-card:hover {
        transform: translateY(-5px);
        border-color: var(--accent);
        box-shadow: 0 5px 20px var(--accent-dim);
    }
    .adv-card h4 {
        color: var(--accent);
        margin-bottom: 10px;
        font-family: 'Orbitron', sans-serif;
        text-transform: uppercase;
        letter-spacing: 2px;
    }
    .adv-card p {
        color: var(--text-gray);
        font-size: 14px;
    }

    @media (max-width: 900px) {
        .advantage-grid { grid-template-columns: 1fr; }
    }
</style>