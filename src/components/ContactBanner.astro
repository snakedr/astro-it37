---
---
<section id="contact-block" class="contact-banner">
    <h2>Связаться с проектным отделом</h2>
    <p>
        **Телефон:** +7(905)105-45-29 | **E-mail:** ssv37@bk.ru
    </p>
    <a href="/contacts" class="btn">Заказать обратный звонок</a>
</section>

<style>
    .contact-banner { 
        background: var(--bg-panel); 
        color: var(--text-main); 
        padding: 35px 50px; 
        text-align: center; 
        margin-top: 80px; 
        border: 1px solid var(--accent);
    }
    .contact-banner h2 { 
        color: var(--accent); 
        margin-bottom: 15px; 
        font-family: 'Orbitron', sans-serif;
        text-transform: uppercase;
        letter-spacing: 2px;
    }
    .contact-banner p { 
        color: var(--text-gray); 
        margin-bottom: 25px; 
        font-size: 16px; 
    }
    .contact-banner a.btn { 
        background: var(--bg-deep); 
        color: var(--accent); 
        border: 1px solid var(--accent); 
        padding: 16px 32px;
        text-decoration: none;
        font-family: 'Orbitron', sans-serif;
        font-weight: 700;
        display: inline-block;
        transition: 0.4s;
    }
    .contact-banner a.btn:hover { background: #333; }
</style>