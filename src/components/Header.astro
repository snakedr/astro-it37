<header>
    <div class="container nav-flex">
        <div class="logo">IT<span>37</span></div>
        
        <button class="menu-toggle" aria-label="Открыть меню">
            <span class="bar"></span><span class="bar"></span><span class="bar"></span>
        </button>
        
        <nav class="nav-links">
            <a href="/" class="active">Главная</a>
            <a href="/design">Проектирование</a>
            <a href="/montazh">Монтаж</a>
            <a href="/control">Диспетчеризация</a>
            <a href="/metrologia">Метрология</a>
            <a href="/contacts">Контакты</a>
        </nav>
        <a href="/contacts" class="btn primary desktop-only">Запрос на аудит</a>
    </div>
</header>

<script>
    // JS работает только для этого компонента
    const menuToggle = document.querySelector('.menu-toggle');
    const navLinks = document.querySelector('.nav-links');

    if(menuToggle && navLinks) {
        menuToggle.addEventListener('click', () => {
            navLinks.classList.toggle('open');
            menuToggle.classList.toggle('open');
        });
    }
</script>

<style>
    /* CSS только для хедера */
    header {
        padding: 20px 0; border-bottom: 1px solid var(--border);
        backdrop-filter: blur(10px); position: sticky; top: 0; z-index: 100; background: rgba(5,5,7,0.8);
    }
    .nav-flex { display: flex; justify-content: space-between; align-items: center; }
    .logo { font-size: 24px; font-weight: 900; color: white; font-family: 'Orbitron'; z-index: 101; }
    .logo span { color: var(--accent); }
    .nav-links { display: flex; gap: 40px; }
    .nav-links a { color: var(--text-gray); transition: 0.3s; text-transform: uppercase; font-weight: 500; font-size: 14px; }
    .nav-links a:hover, .nav-links a.active { color: var(--accent); }
    
    /* Бургер меню */
    .menu-toggle { display: none; background: transparent; border: none; cursor: pointer; padding: 10px; z-index: 101; margin-left: auto; }
    .bar { display: block; width: 25px; height: 3px; margin: 5px 0; background-color: var(--accent); transition: 0.4s; }

    @media (max-width: 900px) {
        .nav-links {
            position: fixed; top: 0; left: 0; width: 100%; height: 100vh;
            background: var(--bg-deep); padding-top: 100px; flex-direction: column;
            transform: translateX(100%); transition: transform 0.4s ease-in-out;
        }
        .nav-links.open { transform: translateX(0); }
        .menu-toggle { display: block; }
        .desktop-only { display: none; }
        .nav-links a { padding: 15px 24px; border-bottom: 1px solid var(--border); width: 100%; display: block; }
    }
</style>