---
// Фронтматтер пуст, так как компонент не принимает props
---
<div class="control-contact-card">
    <div class="card-content">
        <h2>Запрос на подключение к Диспетчеризации</h2>
        <p class="contact-info">
            **Телефон:** +7 (4932) 37-25-55 (доб. 100) | **E-mail:** np@it37.ru
        </p>
        <a href="/contacts" class="btn primary">Получить демо-доступ</a>
    </div>
</div>

<style>
    .control-contact-card {
        margin-top: 80px;
        margin-bottom: 80px; /* Отступ до футера */
        padding: 60px 40px;
        background: var(--bg-panel);
        border: 1px solid var(--accent);
        border-radius: 5px;
        text-align: center;
        position: relative;
        overflow: hidden;
        
        /* Декоративная подсветка */
        box-shadow: 0 0 30px var(--accent-dim);
    }

    .card-content {
        position: relative;
        z-index: 2;
    }

    .control-contact-card h2 {
        color: var(--accent);
        margin-bottom: 15px;
        font-size: 28px;
    }

    .contact-info {
        color: var(--text-gray);
        margin-bottom: 30px;
        font-size: 16px;
    }

    /* Медиа-запросы для адаптивности */
    @media (max-width: 600px) {
        .control-contact-card {
            padding: 40px 20px;
        }
        .control-contact-card h2 {
            font-size: 24px;
        }
        .contact-info {
            font-size: 14px;
        }
    }
</style>