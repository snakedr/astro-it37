---
---
<h2 style="margin-top: 80px;">Ключевые преимущества AI-Диспетчеризации</h2>

<div class="features-grid">
    
    <div class="feature-card">
        <div class="icon">📈</div>
        <h3>Прогнозирование Аномалий</h3>
        <p>
            Алгоритмы машинного обучения анализируют исторические данные и выявляют паттерны, предсказывая потенциальные сбои в системе до того, как они приведут к аварии или перерасходу.
        </p>
    </div>

    <div class="feature-card">
        <div class="icon">💡</div>
        <h3>Энергоэффективность</h3>
        <p>
            Автоматическая корректировка режимов работы оборудования в зависимости от погодных условий и фактического потребления, что гарантирует оптимальное использование энергоресурсов и снижение затрат.
        </p>
    </div>

    <div class="feature-card">
        <div class="icon">🛡️</div>
        <h3>Снижение Рисков</h3>
        <p>
            Круглосуточный мониторинг и мгновенные оповещения о критических событиях позволяют сократить время реакции персонала с часов до минут и избежать штрафов от РСО.
        </p>
    </div>
</div>

<style>
    .features-grid { 
        display: grid; 
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); /* Адаптивная сетка */
        gap: 30px; 
        margin-top: 50px; 
    }

    .feature-card {
        background: var(--bg-panel); /* Цвет фона из глобальных переменных */
        padding: 40px 30px; 
        border: 1px solid var(--border);
        transition: border-color 0.3s, transform 0.3s;
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        border-radius: 5px;
    }
    .feature-card:hover {
        border-color: var(--accent);
        transform: translateY(-5px);
        box-shadow: 0 10px 20px rgba(0, 242, 255, 0.1);
    }
    .icon {
        font-size: 40px;
        margin-bottom: 15px;
        line-height: 1;
    }
    .feature-card h3 {
        color: var(--accent);
        font-size: 20px;
        margin-bottom: 10px;
        font-family: 'Orbitron', sans-serif;
    }
    .feature-card p {
        color: var(--text-gray);
    }
</style>